{
  "name": "spnr-extension",
  "version": "0.1.0",
  "private": true,
  "description": "spnr browser ad surface — sponsored bar on claude.ai, reusing the existing spnr backend",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test test/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "hash-wasm": "^4.11.0"
  }
}
